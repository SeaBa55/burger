<li>
	<h5>{{burger_name}}</h5>

	<button class="change-state" data-id="{{id}}" data-state="{{avail}}">
		{{#if avail}}Devour me!{{else}}Eat again!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>